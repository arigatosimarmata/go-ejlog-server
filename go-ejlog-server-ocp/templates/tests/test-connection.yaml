apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-ejlog-server-ocp.fullname" . }}-test-connection"
  labels:
    {{- include "go-ejlog-server-ocp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-ejlog-server-ocp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
